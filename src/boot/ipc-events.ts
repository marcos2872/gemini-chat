/**
 * Re-export from centralized IPC channels definition
 * This file exists for backwards compatibility with existing imports
 */
export { IPC_CHANNELS, type IpcChannels } from '../shared/ipc-channels';
